<UserControl x:Class="StudentsCalendar.Desktop.Controls.SlotsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ItemsControl ItemsSource="{Binding}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Grid Height="{DynamicResource SlotHeight}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Rectangle Grid.Row="0" Fill="Red" Height="1"/>
                    <TextBlock Grid.Row="1" Text="{Binding StringFormat=' {0:H:mm}'}"
                               Margin="0,5" HorizontalAlignment="Right" VerticalAlignment="Top" />
                </Grid>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
        <ItemsControl.Template>
            <ControlTemplate TargetType="ItemsControl">
                <ItemsPresenter />
            </ControlTemplate>
        </ItemsControl.Template>
    </ItemsControl>
</UserControl>
